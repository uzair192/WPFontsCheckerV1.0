// WEB SPIDER - WEB SCRAPPER CLASS
const cheerio = require('cheerio');


/*module.exports = GoSpider = (HTML) => {
    // LOAD HTML TO CHEERIO INSTANCE
    const $ = cheerio.load(HTML.data);

    // GET FONTS HTML TAGS BY ATTRIBUTES
    $('html > head > link').map((index, ele) => {
        console.log($(ele).attr('href').trim())
    })
}*/